name: Workflow Builder MFE CI

on:
  push:
    branches: [main, feature/**]
    paths:
      - "web/apps/workflow/**"
      - "web/packages/ui/**"
      - ".github/workflows/workflow-mfe.yml"
  pull_request:
    branches: [main]
    paths:
      - "web/apps/workflow/**"
      - "web/packages/ui/**"

jobs:
  build:
    name: Build Workflow Builder
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web/apps/workflow

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8000

  docker:
    name: Docker Build Workflow
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Build Workflow Builder image
        uses: docker/build-push-action@v5
        with:
          context: web/apps/workflow
          file: web/apps/workflow/Dockerfile
          push: false
          tags: agent-workflow:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
